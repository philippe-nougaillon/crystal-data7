<nav class="navbar navbar-expand-lg bg-body-secondary border-bottom py-3">
  <div class="container-fluid">
    <ul class="navbar-nav">
      <li class="nav-item ms-3">
        <%= link_to root_path, class:"text-decoration-none d-flex align-items-center gap-2" do %>
          <span class="material-symbols-outlined">
            diamond
          </span>
          CrystalDATA
        <% end %>
      </li>
    </ul>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      </ul>
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item mt-1 <%= 'active' if params[:controller] == 'users' %>">
            <%= link_to current_user.email, current_user, title: "Profil utilisateur", class: "text-decoration-none py-2 text-black-50 px-3" %>
          </li>
          <li class="nav-item mt-1 <%= 'active' if params[:action] == 'a_propos' %>">
            <%= link_to "Aide", a_propos_path, title: "À Propos de CrystalDATA", class: "text-decoration-none py-2 text-black-50 px-3" %>
          </li>
          <%= button_to destroy_user_session_path, method: :delete, class:'btn btn-outline-danger btn-sm ms-3', 'data-turbo': false, title: 'Se déconnecter' do %>
            Déconnexion
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>