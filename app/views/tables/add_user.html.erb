<h1 class="text-primary">
  <i class="glyphicon glyphicon-share"></i> 
  Partager la table '<%= @table.name.humanize %>' avec un autre utilisateur
</h1>
<br>

<%= form_for :user, url: '/add_user_do' do %>

  <%= hidden_field_tag :id, params[:id] %>

  <div class="field form-group">
  		<div class="col-sm-5">
		    <h4>Entrez l'adresse mail de l'utilisateur avec qui vous souhaitez pargager cette table.</h4>
        <i><small>Si l'utilisateur n'a pas de compte, <%= link_to "créez-lui un compte maintenant", signup_path %></small></i>
        <br><br>
        <%= text_field_tag :email, nil, class:'form-control'  %>
        <br>
        <%= submit_tag "Partager", class: 'btn btn-outline-success' %>
    </div>
  </div>
<% end %>