<h1 class="text-info my-4">
  Importer
</h1>

<p class="mb-4">
  Le fichier à importer doit être de type <b>CSV</b>, au format <b>UTF-8</b>, avec comme séparateur la <b>virgule</b> (,) et les <b>noms d'attributs sur la première ligne</b>. 
</p>

<%= form_tag({action: "import_do", method: :post}, multipart: true) do %>
  <%= label_tag :upload, "Fichier source", class:'form-label' %>
  <%= file_field_tag :upload, accept: 'text/csv', class: 'form-control form-control-lg', required: true %>
  <%= submit_tag "Importer", class: 'btn btn-outline-primary mt-4' %>
<% end %>
