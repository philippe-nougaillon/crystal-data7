<% content_for :title, "Modifier utilisateur" %>

<h1><%= t('users.update_user') %></h1>

<%= render "form", user: @user %>

<br>

<div>
  <%= link_to t('misc.see'), @user %> |
  <%= link_to t('scaffold.back'), users_path %>
</div>
